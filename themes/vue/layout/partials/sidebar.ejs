<% var selfpagecn = (page.type === 'indexcn' || page.type === 'source_cn' || page.type === 'devcn' || page.type === 'downloadcn' || page.type === 'downloadpccn') %>

<div class="sidebar">
  <div class="sidebar-inner">
    <ul class="main-menu">
      <% if (selfpagecn) { %>
        <%- partial('partials/main_menu_cn', { context: 'sidebar' }) %>
      <% } else { %>
        <%- partial('partials/main_menu', { context: 'sidebar' }) %>
      <% } %>
    </ul>
    <div class="list">
      
      <% if (type !== 'search') { %>
        <%- partial('partials/toc', { type: type }) %>
      <% } %>
    </div>
  </div>
</div>  
